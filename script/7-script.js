/* Trapézio */

var base_maior = parseInt(prompt("valor da base maior: "));
var base_menor = parseInt(prompt("valor da base menor: "));
var altura = parseInt(prompt("valor da altura: "));

var area = (base_maior + base_menor) * altura / 2;

alert("o valor de area é " + area);